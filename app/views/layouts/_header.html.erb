<!-- Sidebar -->
<nav class="navbar navbar-inverse navbar-fixed-top" role="navigation">
    <!-- Brand and toggle get grouped for better mobile display -->
    <div class="navbar-header">
        <button type="button" class="navbar-toggle" data-toggle="collapse" data-target=".navbar-ex1-collapse">
            <span class="sr-only">Toggle navigation</span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
            <span class="icon-bar"></span>
        </button>
        <%= link_to root_path, class: "navbar-brand" do %>
            <span class="glyphicon glyphicon-eye-open"></span>
            Mon <%= "- #{@current_computer.name}" if @current_computer != nil %>
        <% end %>
    </div>

    <!-- Collect the nav links, forms, and other content for toggling -->
    <div class="collapse navbar-collapse navbar-ex1-collapse">
        <% if params[:controller] == "computers" && action_name == "show" %>
            <ul class="nav navbar-nav side-nav">
                <li class="active"><a href="#stats" data-toggle="tab"><span class="glyphicon glyphicon-dashboard"></span> Dashboard</a></li>
                <li><a href="#disk" data-toggle="tab" ><span class="glyphicon glyphicon-hdd"></span> Disks</a></li>
                <li><a href="#network" data-toggle="tab"><span class="glyphicon glyphicon-stats"></span> Network</a></li>
            </ul>
        <% end %>

        <ul class="nav navbar-nav navbar-right navbar-user">
            <% if signed_in? %>
                <li>
                    <%= link_to new_user_path do %>
                        <span class="glyphicon glyphicon-user"></span> Users 
                    <% end %>
                </li>        
                <li>
                    <%= link_to new_computer_path do %>
                         <span class="glyphicon glyphicon-cog"></span> Computers
                    <% end %>
                </li>
                <li class="dropdown user-dropdown">
                    <a href="#" class="dropdown-toggle" data-toggle="dropdown">
                        <span class="glyphicon glyphicon-user"></span><%= current_user.name %> <b class="caret"></b>
                    </a>
                    <ul class="dropdown-menu">
                        <li>
                            <%= link_to user_path(@current_user.id) do %>
                                <span class="glyphicon glyphicon-user"></span> Profile
                            <% end %>
                        </li>
                        <li><a href="#"><span class="glyphicon glyphicon-envelope"></span>  Inbox <span class="badge">7</span></a></li>
                        <li>
                            <%= link_to watchdogs_path do %>
                                <span class="glyphicon glyphicon-cog"></span> Watch dogs
                            <% end %>
                        </li>
                        <li class="divider"></li>
                        <li>
                            <%= link_to signout_path, method: "delete" do %>
                                <span class="glyphicon glyphicon-log-out"></span> Log Out
                            <% end %>
                        </li>
                    </ul>
                </li>
            <% else %>
                <li>
                    <%= link_to signin_path do %>
                        Login <span class="glyphicon glyphicon-log-in"></span>
                    <% end %>
                </li>
            <% end %>
        </ul>
    </div><!-- /.navbar-collapse -->
</nav>






